% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{collect_results}
\alias{collect_results}
\title{Collect Trial Results Across Replicates}
\usage{
collect_results(trials, analysis = NULL)
}
\arguments{
\item{trials}{A \code{Trial} R6 object \strong{or} a \code{list} of \code{Trial} objects
(as returned by \code{\link[=replicate_trial]{replicate_trial()}}).}

\item{analysis}{\code{character} or \code{NULL}. When supplied, only analyses whose
name matches one of these values are included. Defaults to \code{NULL}
(all analyses).}
}
\value{
A \code{data.frame} with columns:
\itemize{
\item \code{replicate} \code{integer} Index of the trial replicate (1-based).
\item \code{timepoint} \code{numeric} Calendar time at which the analysis fired.
\item \code{analysis}  \code{character} Name of the analysis (as given in
\code{analysis_generators} or via a trigger helper).
\item Additional columns from the value returned by each analysis function.
}
}
\description{
Gathers analysis outputs from one or more \code{Trial} objects into a single
tidy data frame. Works with any number of named analyses (e.g., both
an interim and a final) and any number of replicates.
}
\details{
Each analysis function may return either a \code{data.frame} (the standard
pattern) or a named \code{list}; both are coerced to a single-row data frame
and stacked. Analyses that return \code{NULL} or \code{NA} are silently skipped.

When \code{trials} is a single \code{Trial} object (e.g., from a one-off
\code{Trial$new()} + \verb{$run()} call), the \code{replicate} column is always \code{1}.
}
\examples{
# --- replicated trial ---
pop_gens <- list(
  control   = function(n) vector_to_dataframe(rnorm(n)),
  treatment = function(n) vector_to_dataframe(rnorm(n, 0.5))
)
an_gens <- list(
  final = list(
    trigger  = rlang::exprs(sum(!is.na(enroll_time)) >= 20L),
    analysis = function(df, timer) {
      data.frame(mean_ctrl = mean(df$data[df$arm == "control"]))
    }
  )
)
trials <- replicate_trial(
  trial_name = "ex", sample_size = 20L,
  arms = c("control", "treatment"), allocation = c(1, 1),
  enrollment = function(n) rexp(n, 1), dropout = function(n) rexp(n, 0.01),
  analysis_generators = an_gens, population_generators = pop_gens, n = 3
)
run_trials(trials)
collect_results(trials)

# --- filter to a specific analysis name ---
collect_results(trials, analysis = "final")
}
\seealso{
\code{\link[=replicate_trial]{replicate_trial()}}, \code{\link[=run_trials]{run_trials()}}, \link{Trial}.
}
