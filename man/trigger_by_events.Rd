% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triggers.R
\name{trigger_by_events}
\alias{trigger_by_events}
\title{Trigger Analysis When Enough TTE Events Have Occurred}
\usage{
trigger_by_events(
  event_col,
  n_events,
  time_col = "time",
  enroll_col = "enroll_time",
  analysis = NULL,
  name = NULL,
  op = ">="
)
}
\arguments{
\item{event_col}{\code{character} Column holding each subject's event calendar
time. \code{NA} indicates no event has occurred yet.}

\item{n_events}{\code{numeric} Number of events required to fire.}

\item{time_col}{\code{character} Column holding the current trial time.
Defaults to \code{"time"} (the column added by \code{Trial$run()}).}

\item{enroll_col}{\code{character} Column holding each subject's enrollment
time. Defaults to \code{"enroll_time"}.}

\item{analysis}{\code{function} or \code{NULL} Optional analysis function called as
\code{analysis(df, current_time, ...)}. If \code{NULL}, the filtered snapshot is
returned as-is with a warning.}

\item{name}{\code{character} or \code{NULL} Result key. Defaults to
\code{"events_<n_events>"}.}

\item{op}{\code{character} Comparison operator for the event-count threshold.
Must be one of \code{c(">=", "<=", ">", "<", "==", "!=", "\%in\%")}.
Defaults to \code{">="}.}
}
\value{
A \code{\link{Condition}} object.
}
\description{
Builds a \code{\link{Condition}} that fires once a target number of time-to-event
(TTE) endpoint events have been observed by the current trial time.
Enrollment guards are always included: only subjects who are enrolled
before the current time are eligible.
}
\examples{
cond <- trigger_by_events(
  event_col = "pfs_event_time",
  n_events  = 100,
  analysis  = function(df, current_time) {
    data.frame(n_events = sum(!is.na(df$pfs_event_time)), fired_at = current_time)
  }
)
}
\seealso{
\link{Condition}, \code{\link[=condition_calendar_time]{condition_calendar_time()}}, \code{\link[=condition_enrollment_fraction]{condition_enrollment_fraction()}},
\code{\link[=timed_count_trigger]{timed_count_trigger()}}, \code{\link[=notna_trigger]{notna_trigger()}}, \code{\link[=col_trigger]{col_trigger()}}.
}
