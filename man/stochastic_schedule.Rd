% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schedule.R
\name{stochastic_schedule}
\alias{stochastic_schedule}
\title{Generate a Stochastic Enrollment and Dropout Schedule}
\usage{
stochastic_schedule(sample_size, arms, allocation, enrollment, dropout)
}
\arguments{
\item{sample_size}{\code{integer} Trial sample size.}

\item{arms}{\code{character} vector of arm identifiers.}

\item{allocation}{\code{numeric} vector of allocation ratios.}

\item{enrollment}{\code{function} that takes \code{n} and returns \code{n} inter-arrival
times (e.g. \code{function(n) rexp(n, rate = 1)}).}

\item{dropout}{\code{function} that takes \code{n} and returns \code{n} inter-dropout
times.}
}
\value{
\code{data.frame} with columns: \code{time}, \code{arm}, \code{enroller} (always 1),
\code{dropper} (always 0 or 1). One row per subject event, sorted by \code{time}.
}
\description{
Creates a time-indexed schedule of enrollment and dropout events across
arms by sampling inter-event times from user-supplied distribution
functions. Each call produces a different realization, capturing
natural variability in study timelines.
}
\details{
Use this when trial-duration variability is substantively important.
For a fixed, reproducible schedule see \code{\link[=deterministic_schedule]{deterministic_schedule()}}.
}
\examples{
stochastic_schedule(
  sample_size = 100,
  arms = c("A", "B"),
  allocation = c(2, 1),
  enrollment = function(n) rexp(n, rate = 0.5),
  dropout    = function(n) rexp(n, rate = 0.1)
)

}
\seealso{
\code{\link[=deterministic_schedule]{deterministic_schedule()}} for piecewise-constant rates,
\code{\link[=add_timepoints]{add_timepoints()}} to attach to a \code{Timer}.
}
