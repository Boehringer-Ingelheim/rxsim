% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triggers.R
\name{condition_calendar_time}
\alias{condition_calendar_time}
\title{Build a Condition that Fires at a Calendar Time}
\usage{
condition_calendar_time(cal_time, analysis = NULL, name = NULL)
}
\arguments{
\item{cal_time}{\code{numeric} Calendar time at or after which to fire.}

\item{analysis}{\code{function} or \code{NULL} Called as
\code{analysis(df, current_time, ...)}. If \code{NULL}, the filtered snapshot
is returned as-is with a warning.}

\item{name}{\code{character} or \code{NULL} Result key. Defaults to
\code{"cal_time_<cal_time>"}.}
}
\value{
A \code{\link{Condition}} object.
}
\description{
Convenience constructor: wraps \code{Condition$new()} with a
\code{calendar_trigger()}. The returned \code{Condition} should be passed to
\code{Trial$new(conditions = list(...))}.
}
\examples{
cond <- condition_calendar_time(
  cal_time = 12,
  analysis = function(df, current_time) {
    data.frame(n_enrolled = sum(!is.na(df$enroll_time)))
  }
)
}
\seealso{
\link{Condition}, \code{\link[=condition_enrollment_fraction]{condition_enrollment_fraction()}}, \link{Trial},
\code{calendar_trigger()}.
}
