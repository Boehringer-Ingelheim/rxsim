% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Trial.R
\name{Trial}
\alias{Trial}
\title{Trial: Simulate a multi‑arm clinical trial}
\value{
A new \code{Trial} instance.

Updates \code{locked_data} and \code{results} fields.
}
\description{
The \code{Trial} class coordinates one or more \code{Population} objects and a \code{Timer}
to simulate a clinical trial.

At each unique time defined in the trial's \code{Timer}, the \code{Trial}:
\itemize{
\item applies enrollment and dropout updates to each \code{Population}
\item builds a snapshot of all currently enrolled subjects
\item evaluates all conditions in the \code{Timer}
\item stores both the snapshot \code{locked_data} and the analysis outputs \code{results}
}

Use \code{run()} to execute the simulation. Trigger conditions are best added with
helper functions \code{\link[=trigger_by_calendar]{trigger_by_calendar()}} or \code{\link[=trigger_by_fraction]{trigger_by_fraction()}}.
}
\examples{
# Create two populations
popA <- Population$new("A", data = vector_to_dataframe(rnorm(10)))
popB <- Population$new("B", data = vector_to_dataframe(rnorm(12)))

# Create a timer and add timepoints
t <- Timer$new("Timer")
t$add_timepoint(time = 1, arm = "A", dropper = 0L, enroller = 4L)
t$add_timepoint(time = 1, arm = "B", dropper = 0L, enroller = 5L)
t$add_timepoint(time = 2, arm = "A", dropper = 1L, enroller = 2L)
t$add_timepoint(time = 2, arm = "B", dropper = 2L, enroller = 3L)

# Add trigger for final analysis at time 2
trigger_by_calendar(2, t, analysis = function(df, current_time) {
  nrow(df)
})

# Create a trial
trial <- Trial$new(
  name = "ExampleTrial",
  seed = 123,
  timer = t,
  population = list(popA, popB)
)

# Run the simulation
trial$run()

prettify_results(trial$results)


## ------------------------------------------------
## Method `Trial$new`
## ------------------------------------------------

t <- Timer$new(name="simple_timer")
pop <- Population$new(
  name = "simple_pop",
  data = vector_to_dataframe(rnorm(5))
)
pop$set_enrolled(5, 1)
Trial$new(name = "simple_trial", timer=t, population = list(pop))

## ------------------------------------------------
## Method `Trial$run`
## ------------------------------------------------

# Create two populations
popA <- Population$new("A", data = vector_to_dataframe(rnorm(10)))
popB <- Population$new("B", data = vector_to_dataframe(rnorm(12)))

# Create a timer and add timepoints
t <- Timer$new("Timer")
t$add_timepoint(time = 1, arm = "A", dropper = 0L, enroller = 4L)
t$add_timepoint(time = 1, arm = "B", dropper = 0L, enroller = 5L)
t$add_timepoint(time = 2, arm = "A", dropper = 1L, enroller = 2L)
t$add_timepoint(time = 2, arm = "B", dropper = 2L, enroller = 3L)

# Create a trial
trial <- Trial$new(
  name = "ExampleTrial",
  seed = 123,
  timer = t,
  population = list(popA, popB)
)

# Run the simulation
trial$run()

prettify_results(trial$results)
}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{name}}{\code{character} Unique identifier for the trial}

\item{\code{seed}}{\code{numeric} Optional seed for reproducibility. If provided,
\code{set.seed()} is called during initialization.}

\item{\code{timer}}{\code{Timer} A \code{Timer} object describing timepoints and conditions.}

\item{\code{population}}{\code{list} A list of \code{Population} objects, one per arm.}

\item{\code{locked_data}}{\code{list} Snapshots of  subject‑level data at each timepoint.}

\item{\code{results}}{(\code{list}) Analysis outputs for each condition.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-Trial-new}{\code{Trial$new()}}
\item \href{#method-Trial-run}{\code{Trial$run()}}
\item \href{#method-Trial-clone}{\code{Trial$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Trial-new"></a>}}
\if{latex}{\out{\hypertarget{method-Trial-new}{}}}
\subsection{Method \code{new()}}{
Create a new \code{Trial} instance.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Trial$new(
  name,
  seed = NULL,
  timer = NULL,
  population = list(),
  locked_data = list(),
  results = list()
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{name}}{\code{character} Unique identifier for the trial.}

\item{\code{seed}}{\code{numeric} or \code{NULL} Optional random seed. If not \code{NULL},
sets the RNG seed for reproducibility.}

\item{\code{timer}}{\code{Timer} A \code{Timer} object defining timepoints and conditions.}

\item{\code{population}}{\code{list} A list of \code{Population} objects, one for each arm.}

\item{\code{locked_data}}{\code{list} Generated at each Trial$run() call.}

\item{\code{results}}{(\code{list}) Generated at each Trial$run() call.}
}
\if{html}{\out{</div>}}
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{t <- Timer$new(name="simple_timer")
pop <- Population$new(
  name = "simple_pop",
  data = vector_to_dataframe(rnorm(5))
)
pop$set_enrolled(5, 1)
Trial$new(name = "simple_trial", timer=t, population = list(pop))
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Trial-run"></a>}}
\if{latex}{\out{\hypertarget{method-Trial-run}{}}}
\subsection{Method \code{run()}}{
Execute a trial simulation.

At each unique time defined by the trial's \code{Timer}:
\itemize{
\item Apply enrollment and dropout actions to each \code{Population}
\item Build a combined snapshot of all currently enrolled subjects
\item Attach a \code{time} column to the snapshot
\item Evaluate all condition readers via \code{Timer$check_conditions()}
\item Store snapshots and condition outputs under time‑indexed list keys
}
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Trial$run()}\if{html}{\out{</div>}}
}

\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{# Create two populations
popA <- Population$new("A", data = vector_to_dataframe(rnorm(10)))
popB <- Population$new("B", data = vector_to_dataframe(rnorm(12)))

# Create a timer and add timepoints
t <- Timer$new("Timer")
t$add_timepoint(time = 1, arm = "A", dropper = 0L, enroller = 4L)
t$add_timepoint(time = 1, arm = "B", dropper = 0L, enroller = 5L)
t$add_timepoint(time = 2, arm = "A", dropper = 1L, enroller = 2L)
t$add_timepoint(time = 2, arm = "B", dropper = 2L, enroller = 3L)

# Create a trial
trial <- Trial$new(
  name = "ExampleTrial",
  seed = 123,
  timer = t,
  population = list(popA, popB)
)

# Run the simulation
trial$run()

prettify_results(trial$results)
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Trial-clone"></a>}}
\if{latex}{\out{\hypertarget{method-Trial-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Trial$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
