% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triggers.R
\name{condition_enrollment_fraction}
\alias{condition_enrollment_fraction}
\title{Build a Condition that Fires at an Enrollment Fraction}
\usage{
condition_enrollment_fraction(
  fraction,
  sample_size,
  analysis = NULL,
  name = NULL
)
}
\arguments{
\item{fraction}{\code{numeric} Sample fraction (0 < fraction <= 1).}

\item{sample_size}{\code{integer} Target sample size.}

\item{analysis}{\code{function} or \code{NULL} Called as
\code{analysis(df, current_time, ...)}. If \code{NULL}, the filtered snapshot
is returned as-is with a warning.}

\item{name}{\code{character} or \code{NULL} Result key. Defaults to
\code{"frac_<fraction>"}.}
}
\value{
A \code{\link{Condition}} object.
}
\description{
Convenience constructor: wraps \code{Condition$new()} with an
\code{enroll_trigger()}. The returned \code{Condition} should be passed to
\code{Trial$new(conditions = list(...))}.
}
\examples{
cond <- condition_enrollment_fraction(
  fraction    = 0.5,
  sample_size = 100,
  analysis    = function(df, current_time) {
    data.frame(n_enrolled = sum(!is.na(df$enroll_time)))
  }
)
}
\seealso{
\link{Condition}, \code{\link[=condition_calendar_time]{condition_calendar_time()}}, \link{Trial},
\code{enroll_trigger()}.
}
