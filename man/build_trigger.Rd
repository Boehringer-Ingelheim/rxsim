% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triggers.R
\name{build_trigger}
\alias{build_trigger}
\title{Build a Condition from a Safe Trigger Specification}
\usage{
build_trigger(
  trigger,
  analysis = NULL,
  name = NULL,
  cooldown = 0,
  max_triggers = 1L
)
}
\arguments{
\item{trigger}{\code{rxsim_trigger} object created by \code{value_trigger()},
\code{count_trigger()}, \code{enroll_trigger()}, \code{calendar_trigger()}, or by
combining them with \code{&} and \code{|}.}

\item{analysis}{\code{function} or \code{NULL} Optional analysis function called as
\code{analysis(filtered_data, current_time)}. If \code{NULL}, the filtered snapshot
is returned as-is with a warning.}

\item{name}{\code{character} or \code{NULL} Result key.}

\item{cooldown}{\code{numeric} Minimum time between consecutive triggers.}

\item{max_triggers}{\code{integer} Maximum number of times the condition may fire.}
}
\value{
A \code{\link{Condition}} object.
}
\description{
Convert an \code{rxsim_trigger} object into a \code{\link{Condition}} using a restricted,
recursively assembled quosure representation.
}
\examples{
trig <- enroll_trigger(0.5, 200) & calendar_trigger(52)
cond <- build_trigger(
  trig,
  analysis = function(df, current_time) nrow(df),
  name = "interim"
)
}
\seealso{
\code{\link[=trigger_by_calendar]{trigger_by_calendar()}}, \code{\link[=trigger_by_fraction]{trigger_by_fraction()}}, \link{Condition}.
}
